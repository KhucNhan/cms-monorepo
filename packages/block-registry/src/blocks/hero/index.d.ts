import { heroSchema, type HeroData } from './schema';
import type { BlockDefinition } from '../../types';
export declare const heroBlock: BlockDefinition<typeof heroSchema>;
export { heroSchema, type HeroData };
